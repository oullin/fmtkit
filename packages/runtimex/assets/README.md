Runtime archives are generated by release tooling.

Expected archive names:

- runtime-darwin-arm64.tar.gz
- runtime-linux-amd64.tar.gz
- runtime-linux-arm64.tar.gz

Each archive is extracted under the versioned runtime cache and must provide
`bin/fmt-ts` and `bin/fmt-lint`.
