/fmt
*.bun-build

# TS toolchain assets staged per platform by stage-ts-assets.sh, next to the
# package that embeds them.
/packages/go/driver/internal/typescript/embedded/bin/
/storage/bin*/
/storage/dist/
/storage/dist-test/
/storage/.cache/
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
node_modules/

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Runtime config. The repository root's own config.yml is committed: it is
# where fmtkit's complexity baseline lives, and it is what makes `make check`
# green on a clean tree.
config.yml
!/config.yml

.claude/*
!.claude/.gitkeep
