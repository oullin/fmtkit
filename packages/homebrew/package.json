{
	"name": "homebrew",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "pnpm typecheck && pnpm test",
		"generate": "node ./scripts/generate-formula.ts",
		"test": "node --test ./test/*.test.ts",
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "25.9.1",
		"typescript": "6.0.3"
	}
}
